{
    "version": 2,
    "buildCommand": "npm install -g node-gyp && npm run vercel-build",
    "builds": [
        {
            "src": "index.js",
            "use": "@vercel/node",
            "buildCommand": "npm install -g node-gyp && npm run vercel-build"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "index.js"
        }
    ]
}